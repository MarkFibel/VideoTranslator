# Быстрое тестирование восстановления сессии

## Шаг 1: Запустите сервер

```powershell
python -m src.app
```

## Шаг 2: Откройте страницу в браузере

```
http://localhost:8000
```

## Шаг 3: Проверьте эндпоинт статуса (опционально)

Откройте в новой вкладке или через curl:

```
http://localhost:8000/files/session/status
```

Должен вернуться JSON:
```json
{
  "pending": false,
  "need_download": false,
  "file": null
}
```

## Шаг 4: Загрузите файл

1. Выберите любой видео файл
2. Нажмите "Отправить видео"
3. Дождитесь завершения обработки
4. Должна появиться кнопка "Скачать видео"

## Шаг 5: Обновите страницу (F5)

После обновления страницы должно произойти:
- ✅ Кнопка "Скачать видео" остается видимой
- ✅ Отображается информация о файле (имя, размер, время)
- ✅ Файл можно скачать по клику на кнопку

## Шаг 6: Проверьте консоль браузера

Откройте DevTools (F12) → Console и проверьте логи:

```
🔄 FormController.restoreSessionState - начало восстановления
📦 Session data: {pending: false, need_download: true, file: {...}}
✅ Файл готов к скачиванию
📎 FormController.restoreFileInfo {filename: "...", size: ..., upload_time: "..."}
🔄 FileController.restoreFromSession {filename: "...", size: ..., upload_time: "..."}
✅ Файл восстановлен из сессии
```

## Проверка различных состояний

### Состояние 1: Нет файла
- Обновите страницу когда нет загруженного файла
- Должна показаться обычная форма загрузки

### Состояние 2: Обработка в процессе (симуляция)
Временно измените в backend `session['pending'] = True` и обновите страницу
- Должен показаться спиннер "Обработка файла..."

### Состояние 3: Файл готов (текущая реализация)
- Загрузите файл → дождитесь завершения → обновите страницу
- Должна показаться кнопка скачивания с информацией о файле

## Возможные проблемы

### Проблема: "Session data: {pending: false, need_download: false, file: null}"
**Решение:** Cookie сессии истек или был удален. Загрузите файл заново.

### Проблема: Кнопка скачивания не появляется
**Решение:** 
1. Проверьте консоль браузера на ошибки
2. Проверьте ответ `/files/session/status` в Network tab
3. Убедитесь, что `need_download: true` в ответе

### Проблема: Информация о файле не отображается
**Решение:**
1. Проверьте что `file` объект присутствует в ответе
2. Убедитесь что `fileController` правильно инициализирован
3. Проверьте консоль на warnings: `fileController не найден`

## Альтернативный тест через curl

```powershell
# Получить cookie сессии (после загрузки файла в браузере)
# Откройте DevTools → Application → Cookies → скопируйте значение 'session'

$session = "ваш_session_cookie"
curl -H "Cookie: session=$session" http://localhost:8000/files/session/status
```

Ожидаемый результат:
```json
{
  "pending": false,
  "need_download": true,
  "file": {
    "filename": "your_video.mp4",
    "size": 10485760,
    "upload_time": "2025-11-11T12:34:56.789Z"
  }
}
```
